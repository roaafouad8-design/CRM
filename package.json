{
  "name": "crm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && echo \"Placeholder build output\" > dist/index.html",
    "start": "echo \"No start command defined\""
  }
}

